# Offline training only. Deliberately NOT in the project's requirements.txt: the agent image
# serves the API, the UI and the ingest job, and none of them needs scikit-learn. Training emits
# plain coefficients as JSON and src/scoring_model.py evaluates them in pure Python, so ~120MB of
# numerical stack stays out of the shipped image and "sklearn failed to import at runtime" is not
# a failure mode that can occur.
scikit-learn==1.5.2
numpy==2.1.3
scipy==1.14.1
